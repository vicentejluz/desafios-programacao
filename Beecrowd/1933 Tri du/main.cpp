#include <iostream>

using namespace std;

int main()
{
    int a, b;

    cin >> a >> b;

    (a >= b) ? cout << a << endl : cout << b << endl; // operador ternário (condição ? true : false)

    return 0;
}